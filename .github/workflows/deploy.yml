# GitHub Actions 自动部署工作流（可选）
# 如果需要自动构建，取消注释下面的内容

# name: Deploy to GitHub Pages
# 
# on:
#   push:
#     branches: [ main ]
# 
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v3
#       - name: Setup Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: '18'
#       - name: Build
#         run: npm run build
#       - name: Deploy
#         uses: peaceiris/actions-gh-pages@v3
#         with:
#           github_token: ${{ secrets.GITHUB_TOKEN }}
#           publish_dir: ./
